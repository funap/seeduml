{
  "name": "nanouml",
  "version": "0.1.0",
  "description": "PlantUML compatible diagram generator for the web",
  "main": "dist/nanouml.js",
  "module": "dist/nanouml.esm.mjs",
  "browser": "dist/nanouml.js",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "npm run type-check && esbuild src/index.ts --bundle --outfile=dist/nanouml.js --platform=browser --format=iife --global-name=nanouml && esbuild src/index.ts --bundle --minify --outfile=dist/nanouml.min.js --platform=browser --format=iife --global-name=nanouml && esbuild src/index.ts --bundle --outfile=dist/nanouml.esm.mjs --platform=browser --format=esm && esbuild src/index.ts --bundle --minify --outfile=dist/nanouml.esm.min.mjs --platform=browser --format=esm",
    "test": "vitest run"
  },
  "keywords": [
    "plantuml",
    "sequence-diagram",
    "svg",
    "uml"
  ],
  "author": "Atsushi Funakoshi",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  }
}